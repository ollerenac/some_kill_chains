---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-06-16T00:03:58.819Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified during this session._

<current_state>
Project is at **v1.0 complete** (4 phases done, 23 scenarios, kill-chains EN+ES, README tabla, flag sanitization). All recent work has been ad-hoc quick tasks. Last two sessions produced:
- `scripts/generate_pdf_es.py` — PDF generator (reportlab, NotoSans, 14 pages, 91 KB)
- `docs/CTF-CURSOS.md` — mapping 23 CTFs to 16 curriculum courses (quick task 260615-m33, committed ec82692)

The git author history still shows "blinkomaniak" for all historical commits. This has NOT been fixed yet. The fix was attempted last session but failed due to unstaged `.planning/config.json`. See **Human actions pending**.
</current_state>

<completed_work>

**This session (2026-06-15):**
- Task 1: Generated `scenarios_es.pdf` (14 pages) using reportlab from `SCENARIOS_ES.md`. Script at `scripts/generate_pdf_es.py`. Fonts: NotoSans TrueType. Colour-coded difficulty badges, reference tables, domain section headers.
- Task 2: Created `docs/CTF-CURSOS.md` — bidirectional mapping: CTF→course (primary + secondary + rationale) and course→CTF list. Priority contact table for professors. Quick task 260615-m33, committed ec82692.

**Previous sessions (2026-06-13):**
- Quick task 260613-5wu: Sanitized SCENARIOS.md + SCENARIOS_ES.md (removed all flag-location hints). Created SCENARIOS_INTERNAL.md + SCENARIOS_ES_INTERNAL.md as instructor copies (gitignored).
- CLAUDE.md and .gitignore untracked from public repo (both now local-only, gitignored).
- README.md updated with 23-scenario summary table.
</completed_work>

<remaining_work>

**Explicitly requested and not yet done:**
1. Fix git author history (blinkomaniak → ollerenac) — requires human action (see below)

**Untracked files to decide on:**
- `scripts/generate_pdf_es.py` — should be committed to repo
- `escenarios_es.pdf` / `scenarios_es.pdf` — binary artifact; recommend gitignoring `*.pdf`
- `.planning/README.md` — planning docs; commit or gitignore
- `.planning/phases/04-llm-atp-chain-kill-chains-final-review/04-VERIFICATION.md` — untracked phase artifact; commit or ignore
- `BRIEF.md` — local doc; decide to commit or gitignore
- `docs/contenidos_adicionales_inictel/`, `docs/curricula/`, `docs/images/` — local reference material; not committed
- `.claude/` — settings.json already tracked; directory appears untracked because of new files inside

**Future work (no user request yet):**
- Generate EN version of PDF (same script, adapt for SCENARIOS.md)
- Add GitHub Pages link for `docs/CTF-CURSOS.md` to README
</remaining_work>

<decisions_made>

- PDF generation: Option A (reportlab, Python, zero new installs) chosen by user over pandoc or weasyprint
- PDF scope: Spanish only (`SCENARIOS_ES.md`), public version (no flag hints)
- CTF→course mapping: affinity based on *central technique* of each CTF, not general domain
- 4 courses with no CTF fit: CBS02, CBS04, CBS08, BMA20 (DB and early OS courses)
</decisions_made>

<blockers>

- **Git author history**: All 108 commits show "blinkomaniak" as author. Fix requires running rebase with stash (unstaged `.planning/config.json` blocks the rebase). See human_actions_pending.
</blockers>

## Required Reading (in order)
1. `docs/CTF-CURSOS.md` — mapping created this session; needed if continuing professor outreach work
2. `scripts/generate_pdf_es.py` — PDF generator; needed if modifying the PDF output
3. `.planning/STATE.md` — overall project state and quick task history

## Infrastructure State
- **reportlab PDF**: `scenarios_es.pdf` at repo root (untracked). Run `python3 scripts/generate_pdf_es.py` to regenerate.
- **NotoSans fonts**: `/usr/share/fonts/truetype/noto/NotoSans-{Regular,Bold,Italic,BoldItalic}.ttf` — required by script.
- **GitHub Pages**: `https://ollerenac.github.io/some_kill_chains/` — public repo deployed.

## Human Actions Pending

**Fix git author history (blinkomaniak → ollerenac):**
All commits currently show the wrong author. The global git config was corrected (`ollerenac` / `ollerenac@uni.edu.pe`) but historical commits still show `blinkomaniak`. To fix:

```bash
# In /home/researcher/Research/cont_adic/
git stash                  # stash .planning/config.json modifications
git rebase --root --exec 'git commit --amend --reset-author --no-edit'
git stash pop
git push origin main --force
```

Warning: `--force` push rewrites public history. Coordinate if others have cloned the repo.

<context>
This is a post-v1.0 session. The project catalog is complete. Recent work has been producing derivative artifacts: a PDF export and a professor-contact mapping. The user's workflow is:
1. Research/propose content (done in v1.0)
2. Generate shareable documents from that content (PDF, mapping tables)
3. Use those documents to approach professors/coordinators

The next likely request will be either: more artifact generation (EN PDF, formatted tables), professor outreach support, or new scenario proposals.
</context>

<next_action>
Start with: commit `scripts/generate_pdf_es.py` and decide whether `*.pdf` should be gitignored or committed as a release artifact.
</next_action>
