---
context: phase
phase: 01-scenario-proposals-document
task: 1
total_tasks: 1
status: complete_awaiting_human_approval
last_updated: 2026-06-11T17:04:11.874Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> No constraints were discovered through failure this session.
> Remove this section when resuming if still clean.

_No blocking anti-patterns recorded. Proceed directly to current state._

## Critical Anti-Patterns

_None encountered this session._

<current_state>
Phase 1 execution is 100% complete. SCENARIOS.md has been authored, code-reviewed, all Critical + Warning findings fixed, and the verification report written. The phase is sitting at the human approval gate — the instructor must read SCENARIOS.md and approve each scenario before Phase 2 (kill-chains) can begin.

Git is clean on `master`. The only uncommitted file is `CLAUDE.md` — this change is unrelated to phase work and should NOT be committed with phase artifacts.

Last commit: `81db4a2` — fix(01): apply code review fixes to SCENARIOS.md
</current_state>

<completed_work>

All phase work is done:

- Task 1: Author all 23 CTF scenario proposals — **Done** (commit `3030b44`)
- Post-execution: worktree merged, STATE.md + ROADMAP.md updated (commit `1408cd7`)
- Code review: 8 findings produced in `01-REVIEW.md` (commit `cd6d4de`)
- Fixes applied: CR-01 (ATP-04 LotL framing), CR-02 (NET-03 HTTP→HTTPS), WR-01 (CVE-04 API name), WR-02 (AD-05 stage labels), WR-03 (ATP-04 second protocol), WR-04 (ATP→APT header) — commit `81db4a2`
- Verification: `01-VERIFICATION.md` written with `human_needed` status — all 15 automated assertions pass (commit `bc434db`)
</completed_work>

<remaining_work>

Phase 1 tasks: **none** — all tasks complete.

**Human approval gate (blocking Phase 2):**
- Read `SCENARIOS.md` end-to-end as a lab instructor
- For each of the 23 entries, decide: approve / revise / reject
- Confirm the document is self-contained (no need to read REQUIREMENTS.md to evaluate an entry)
- Once approved, say so — Phase 2 can then begin with `/gsd:execute-phase 2`

If any scenarios need revision, apply edits to `SCENARIOS.md` and re-run the acceptance suite:
```bash
grep -c "^### " SCENARIOS.md            # expected: 23
grep -cP "T\d{4}" SCENARIOS.md          # expected: 0
grep -c "release_agent" SCENARIOS.md    # expected: 0
grep -c "CVE-2021-1675" SCENARIOS.md    # expected: 1
grep -c "CVE-2021-34527" SCENARIOS.md   # expected: 1
```
</remaining_work>

<decisions_made>

- **CVE-04 framing**: LPE path only (not RCE). Title references both CVE-2021-1675 (LPE) and CVE-2021-34527 (RCE) per RESEARCH.md Pitfall 1. 2-VM standalone design requires a pre-existing foothold, which is the LPE path.
- **CC-02 cgroup**: Described as "cgroup notification path" only — no "release_agent" string. Phase 3 kill-chain detail.
- **ATP-04 constraint**: "No-malware constraint" (no compiled payloads, no shellcode, no persistent implants) — NOT living-off-the-land. mitm6/ntlmrelayx/evil-winrm are external attacker tools, not OS-native binaries.
- **Section header**: "Multi-Step APT Chains" (corrected from ATP). ATP- prefix on scenario IDs is locked in REQUIREMENTS.md and unchanged.
- **AD-05 midpoint language**: "establishing a foothold that yields the first flag" / pivot position secured → crack → second flag. No "first phase / second phase" stage labels.
- **Tool naming rule**: Key learning tools named in narratives (Responder, BloodHound, Certipy, Rubeus, Hashcat, mitm6, evil-winrm, kubectl). Generic tools (nmap, curl) omitted.
</decisions_made>

<blockers>

None. The only outstanding item is the human approval gate (not a technical blocker).
</blockers>

## Required Reading (in order)

1. `SCENARIOS.md` — the actual deliverable; read end-to-end before doing anything else
2. `.planning/phases/01-scenario-proposals-document/01-VERIFICATION.md` — 15 automated checks all pass; 1 human check pending (self-containedness)
3. `.planning/phases/01-scenario-proposals-document/01-REVIEW.md` — code review; 2 Info findings intentionally skipped (IN-01 mysmb.py backtick, IN-02 NET-03 difficulty) — revisit if the instructor raises them during approval
4. `.planning/REQUIREMENTS.md` — authoritative metadata for all 23 scenarios (difficulty, VM count, IDs); use this if any scenario needs revision

## Critical Anti-Patterns (do NOT repeat these)

- **False LotL claim**: Do not frame any scenario as "living-off-the-land" unless all tools used are genuinely OS-native. mitm6, ntlmrelayx, evil-winrm, Responder, Certipy, etc. are all external attacker tools. LotL = WMI, certutil, netsh, PowerShell built-ins, LOLBins.
- **Phase 1 kill-chain leakage**: No Windows API names, no specific CVE exploitation steps, no numbered attack sequences in SCENARIOS.md. Proposals describe *what* the student does and *obtains* — not *how* at the implementation level. That detail belongs in Phase 2.

## Infrastructure State

- No running services or background processes
- `CLAUDE.md` has uncommitted modifications — unrelated to phase work; do not commit with phase artifacts
- `BRIEF.md` and `docs/` are untracked — not part of phase work

<context>
Phase 1 is a documentation-only phase (SCENARIOS.md is a markdown proposals catalog). All technical work is narrative writing, not code. The acceptance criteria suite uses grep assertions. Phase 2 shifts to kill-chain authoring — much more technically detailed per-scenario work.

The executor agent produced clean output on the first run. The code review caught meaningful issues (the LotL/ATP framing errors were substantive, not stylistic). All fixes were applied and verified before pausing.
</context>

<next_action>
No code/documentation work needed. The next action is human: read SCENARIOS.md end-to-end, approve the 23 scenarios (or note which need revision), and give the go-ahead for Phase 2. Once approved, run `/gsd:execute-phase 2`.
</next_action>
